# agora-react-uikit@1.2.0 declares peer react ^16/17/18 but this app runs
# React 19 — it works at runtime, but npm's strict peer resolution refuses to
# install (ERESOLVE). Vercel reads this file, so deploys install the same way
# local installs do.
legacy-peer-deps=true
